services:
    torboxarr:
        image: mrjoiny/torboxarr:latest
        restart: unless-stopped
        ports:
            - "8085:8085"
        env_file:
            - ../.env
        volumes:
            - ../data:/data
            - torboxarr_config:/config

volumes:
    torboxarr_config:
